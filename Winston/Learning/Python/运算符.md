# 定义
对操作数进行运算的符号。
# 七种
### 算术运算符、关系（比较）运算符、赋值运算符、逻辑运算符、位运算符、成员运算符、身份运算符。
# 运算符之间存在优先级
![[Pasted image 20230209210410.png]]
共13级
## 算术运算符，共七种
加、减、乘、除、取模（求余数）、幂、取整，
![[Pasted image 20230209210803.png]]
## 比较运算符，共七种，（Python3中为六种，弃”<>不等于“）
等于、不等于、大于、小于、大于等于、小于等于
![[Pasted image 20230209211018.png]]
##  赋值运算符，共八种
等于、加赋值，减法赋值、乘赋值、除赋值、取模（余数）赋值、幂赋值，取整除赋值
![[Pasted image 20230209211256.png]]
## 位运算符，共六种
位与（一一得一）、位或（有一得一）、位异或（一零得一）、位取反（零一互变）、左移动（高位丢弃，低位补0。）、右移动运算符（高位补零、低位挤出？）。
![[Pasted image 20230209211439.png]]
## 逻辑运算符，共三种
与、或、非，
![[Pasted image 20230209212427.png]]
## 成员运算符，共两种
in 在（是、属于），not in 不在（不是、不属于）
![[Pasted image 20230209212551.png]]
##  身份运算符，共两种
is 是，is not不是

![[Pasted image 20230209212709.png]]
is 与 == 区别：
is 用于判断两个变量引用对象是否为同一个(同一块内存空间)， == 用于判断引用变量的值是否相等。

